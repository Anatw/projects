int MaxDifference(int array[], int length)
{
    int left = 1;
    int left_temp_sum = array[0];
    int left_sum = array[0];

    int right = length - 1;
    int right_temp_sum = array[length];
    int right_sum = array[length];

    while (left < right)
    {
        if ((left_sum + array[left]) - (right_sum) >)
        {
        }
    }
}
