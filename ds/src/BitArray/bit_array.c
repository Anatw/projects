#include <stdlib.h>

#include "bit_array.h"

char *ToString(bit_array_t array, char *dest)
{
	
}

bit_array_t SetAll()
{
	/*bit_array_t bit_array = 1;
	bit_array = ~ (bit_array & 0);*/
	return (~0);
}
